# In order to to create pages it is necessary to define routes for them. A route
# maps a URL path to a controller. It defines with what function or method will
# be called when a URL is accessed. The following lines defines three of them
# for this module.

# Menu items corresponding to these URLs are defined separately in the
# page_example.menu_links.yml file.

# If the user accesses http://example.com/?q=examples/page-example, the routing
# system will look for a route with that path. In this case it will find a
# match, and execute the _controller callback. In this case the callback is
# defined as a classname
# ("\Drupal\page_example\Controller\PageExampleController") and a method
# ("description").

stanford_capx.settings:
  path: 'admin/config/capx'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\AdminPagesController::settingsPage'
    _title: 'CAPx Settings'
  requirements:
    _permission: 'administer stanford_capx'

stanford_capx.authentication_credentials:
  path: 'admin/config/capx/authentication'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\AdminPagesController::authenticationPage'
    _title: 'Authentication Credentials'
  requirements:
    _permission: 'administer stanford_capx'

stanford_capx.test:
  path: 'admin/config/capx/test'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\TestPageController::test'
    _title: 'CAPx Test Page'
  requirements:
    _permission: 'administer stanford_capx'

stanford_capx.mappings:
  path: 'admin/config/capx/mappings'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\AdminPagesController::mappingsPage'
    _title: 'CAPx Mappings'
  requirements:
    _permission: 'administer stanford_capx'

stanford_capx.create_mapping:
  path: 'admin/config/capx/mappings/create'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\AdminPagesController::createMappingPage'
    _title: 'Create new CAPx mapping'
  requirements:
    _permission: 'administer stanford_capx'

stanford_capx.import:
  path: 'admin/config/capx/import'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\AdminPagesController::importPage'
    _title: 'CAPx Import'
  requirements:
    _permission: 'administer stanford_capx'

stanford_capx.profiles:
  path: 'admin/config/capx/profiles'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\AdminPagesController::profilesPage'
    _title: 'CAPx Profiles'
  requirements:
    _permission: 'administer stanford_capx'

stanford_capx.help:
  path: 'admin/config/capx/help'
  defaults:
    _controller: '\Drupal\stanford_capx\Controller\AdminPagesController::helpPage'
    _title: 'CAPx Help'
  requirements:
    _permission: 'administer stanford_capx'

